# Keep Omarchy no-gaps mode, but restore a visible focus indicator.
# Loaded after Omarchy toggle flags so it overrides window-no-gaps border_size = 0.
general {
    border_size = 2

    # Let the active border come from the current theme. Hide inactive borders.
    col.inactive_border = rgba(00000000)
}
